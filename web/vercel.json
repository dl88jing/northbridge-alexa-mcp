{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/((?!control$).*)", "destination": "/api/control?nb_path=$1" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
